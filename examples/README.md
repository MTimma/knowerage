# Example files

- **`registry.sample.json`** — Illustrative contents for **`knowerage/registry.json`** at the workspace root (not `knowerage.json`; see [contracts/contracts.md](../contracts/contracts.md)). The live file is produced by `knowerage_reconcile_record` / `knowerage_reconcile_all`; keys are analysis paths, values are registry records.
